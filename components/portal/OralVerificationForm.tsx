"use client";
import { useRouter } from "next/navigation";
import { useState, type FormEvent } from "react";
export function OralVerificationForm({ makeupId }: { makeupId: string }) { const router = useRouter(); const [message, setMessage] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); const response = await fetch(`/api/facilitator/makeup/${makeupId}/oral-verification`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ status: form.get("status"), note: form.get("note") }) }); const data = await response.json().catch(() => ({})); setMessage(response.ok ? "Academic verification saved." : data.message || "Verification failed."); if (response.ok) router.refresh(); } return <form onSubmit={submit} className="mt-4 space-y-3 border-t border-white/10 pt-4"><select name="status" className="min-h-11 w-full rounded-xl border border-white/20 bg-[#0b2140] px-3 text-white"><option value="scheduled">Scheduled</option><option value="completed">Completed</option><option value="satisfactory">Satisfactory</option><option value="not_satisfactory">Not Satisfactory</option><option value="reschedule_required">Reschedule Required</option></select><textarea name="note" required rows={3} placeholder="Brief academic verification note" className="w-full rounded-xl border border-white/20 bg-white/10 p-3 text-white placeholder:text-white/50" /><button className="rounded-xl bg-[var(--realm-gold)] px-4 py-2 font-semibold text-[#071327]">Save Oral Verification</button>{message ? <p className="text-sm text-white/75">{message}</p> : null}</form>; }

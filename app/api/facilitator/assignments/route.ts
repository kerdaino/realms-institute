import { NextResponse } from "next/server";
import { fetchAdminAssignments, fetchAssessmentOptions } from "@/lib/lms/assessmentData";
import { saveAssignment } from "@/lib/lms/assessmentService";
import { lmsApiError, readJsonObject } from "@/lib/lms/apiResponse";
import { resolveFacilitatorAssessmentContext } from "@/lib/lms/facilitatorAssessments";
export async function GET() { try { const context = await resolveFacilitatorAssessmentContext(); const [all, options] = await Promise.all([fetchAdminAssignments(context.supabase), fetchAssessmentOptions(context.supabase)]); return NextResponse.json({ assignments: all.filter((item) => context.offeringIds.includes(String(item.cohort_course_id))), options: { offerings: options.offerings.filter((item) => context.offeringIds.includes(item.id)), sessions: options.sessions.filter((item) => context.offeringIds.includes(item.cohort_course_id)) } }); } catch (error) { return lmsApiError(error, "Assigned assessments could not be loaded."); } }
export async function POST(request: Request) { const body = await readJsonObject(request); if (!body) return NextResponse.json({ message: "A valid request body is required." }, { status: 400 }); try { const context = await resolveFacilitatorAssessmentContext(); if (!context.offeringIds.includes(String(body.cohort_course_id))) return NextResponse.json({ message: "You are not assigned to this cohort course." }, { status: 403 }); return NextResponse.json({ assignment: await saveAssignment(context.supabase, body, { actorLabel: "Facilitator", actorUserId: context.userId }) }, { status: 201 }); } catch (error) { return lmsApiError(error, "Assignment draft could not be created."); } }

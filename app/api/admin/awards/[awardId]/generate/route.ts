import { isAdminAuthenticated } from "@/lib/adminAuth"; import { lmsApiError, readJsonObject } from "@/lib/lms/apiResponse"; import { requireLmsAdminClient } from "@/lib/lms/adminData"; import { generateAwardDocument } from "@/lib/lms/awardService";
export async function POST(request: Request, { params }: { params: Promise<{ awardId: string }> }) { if (!(await isAdminAuthenticated())) return Response.json({ message: "Unauthorized." }, { status: 401 }); const [{ awardId }, body] = await Promise.all([params, readJsonObject(request)]); try { return Response.json({ award: await generateAwardDocument(requireLmsAdminClient(), awardId, { actorLabel: String(body?.actor_name || "REALMS Admin") }), message: "Certificate PDF generated and stored privately." }); } catch (error) { return lmsApiError(error, "Certificate document could not be generated."); } }

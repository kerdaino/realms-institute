import { NextResponse } from "next/server";
import { addStudentAbsenceEvidence } from "@/lib/lms/absenceService";
import { isUuid } from "@/lib/lms/adminConstants";
import { lmsApiError, readJsonObject } from "@/lib/lms/apiResponse";
import { requireStudentAbsenceApi } from "@/lib/lms/studentAbsenceApi";
import { storeStudentAbsenceUpload } from "@/lib/lms/privateStorage.server";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { const { id } = await params; if (!isUuid(id)) return NextResponse.json({ message: "Valid evidence metadata is required." }, { status: 400 }); try { const context = await requireStudentAbsenceApi(); if (request.headers.get("content-type")?.includes("multipart/form-data")) { const form = await request.formData(); const candidate = form.get("attachment"); if (!(candidate instanceof File) || candidate.size <= 0) return NextResponse.json({ message: "Choose a supporting-evidence file." }, { status: 400 }); const evidence = await storeStudentAbsenceUpload(context.supabase, context.user.id, id, candidate, { title: String(form.get("title") || ""), description: String(form.get("description") || "") }); return NextResponse.json({ evidence }, { status: 201 }); } const body = await readJsonObject(request); if (!body) return NextResponse.json({ message: "Valid evidence metadata is required." }, { status: 400 }); return NextResponse.json({ evidence: await addStudentAbsenceEvidence(context.supabase, context.user.id, id, body) }, { status: 201 }); } catch (error) { return lmsApiError(error, "Your file could not be uploaded. Please check the file type and size and try again."); } }

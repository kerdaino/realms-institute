import Link from "next/link"; import { AdminShell } from "@/components/admin/AdminShell"; import { StatusBadge } from "@/components/admin/LmsUi"; import { requireAdmin } from "@/lib/adminAuth"; import { requireLmsAdminClient } from "@/lib/lms/adminData"; import { fetchAdminAwards } from "@/lib/lms/graduationData";
export default async function CertificateTemplatesPage() { await requireAdmin(); const data = await fetchAdminAwards(requireLmsAdminClient()); return <AdminShell title="Certificate Templates" description="Only explicitly approved templates with approved signatory names, roles and signature assets may be used for official issuance."><Link href="/admin/awards" className="font-semibold text-amber-800">← Awards</Link><div className="mt-6 grid gap-4">{data.templates.map((template) => <Link key={template.id} href={`/admin/awards/templates/${template.id}`} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-amber-800">{template.award_type} · v{template.template_version}</p><h2 className="mt-2 text-xl font-semibold">{template.template_name}</h2></div><StatusBadge value={template.template_status} /></div><p className="mt-3 text-sm text-slate-600">Signatories: {Array.isArray(template.signature_configuration) ? template.signature_configuration.length : 0} · Background: {template.background_asset_path ? "Configured" : "Not configured"}</p></Link>)}</div></AdminShell>; }

import Link from "next/link";
import { AdminShell } from "@/components/admin/AdminShell";
import { requireAdmin } from "@/lib/adminAuth";
import { fetchAssessmentOverview } from "@/lib/lms/assessmentData";
import { requireLmsAdminClient } from "@/lib/lms/adminData";
export default async function AdminAssessmentsPage() { await requireAdmin(); const metrics = await fetchAssessmentOverview(requireLmsAdminClient()); const cards = [["Assignments Published", metrics.assignmentsPublished], ["Open Assignments", metrics.openAssignments], ["Submissions Awaiting Review", metrics.submissionsAwaitingReview], ["Late Submissions", metrics.lateSubmissions], ["Resubmissions Required", metrics.resubmissionsRequired], ["Quizzes Published", metrics.quizzesPublished], ["Active Quizzes", metrics.activeQuizzes], ["Quiz Attempts Awaiting Review", metrics.quizAttemptsAwaitingReview]]; return <AdminShell title="Assessments Overview" description="Operational assessment workload only. Final School of Discovery scoring remains reserved for BUILD 11."><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([label, value]) => <article key={String(label)} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm text-slate-600">{label}</p><p className="mt-2 text-3xl font-semibold text-[#071327]">{value}</p></article>)}</div><div className="mt-6 flex gap-3"><Link href="/admin/assignments" className="rounded-xl bg-[#071327] px-5 py-3 font-semibold text-white">Assignments</Link><Link href="/admin/quizzes" className="rounded-xl border border-slate-300 bg-white px-5 py-3 font-semibold">Quizzes</Link></div></AdminShell>; }

import Link from "next/link"; import { AnnouncementForm } from "@/components/admin/Build12Actions"; import { AdminShell } from "@/components/admin/AdminShell"; import { StatusBadge } from "@/components/admin/LmsUi"; import { requireAdmin } from "@/lib/adminAuth"; import { requireLmsAdminClient } from "@/lib/lms/adminData"; import { fetchAdminAnnouncements } from "@/lib/lms/graduationData"; import { humanizeResult } from "@/lib/lms/results";
export default async function AdminAlumniAnnouncementsPage() { await requireAdmin(); const announcements = await fetchAdminAnnouncements(requireLmsAdminClient()); return <AdminShell title="Alumni Announcements" description="Target current alumni by cohort, completed discipleship route or completed skill pathway. Draft and expired announcements remain hidden from alumni."><Link href="/admin/alumni" className="font-semibold text-amber-800">← Alumni</Link><div className="mt-6"><AnnouncementForm /></div><div className="mt-6 grid gap-4">{announcements.map((announcement) => <article key={announcement.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-wide text-amber-800">{humanizeResult(announcement.announcement_type)} · {humanizeResult(announcement.target_scope)}</p><h2 className="mt-2 text-xl font-semibold">{announcement.title}</h2></div><StatusBadge value={announcement.announcement_status} /></div><p className="mt-2 text-sm text-slate-600">{announcement.summary}</p><p className="mt-3 text-xs text-slate-500">Target: {announcement.target_value ?? "All alumni"} · Reads: {(announcement.alumni_announcement_reads ?? []).length}</p></article>)}</div></AdminShell>; }

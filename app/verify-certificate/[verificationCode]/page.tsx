import type { Metadata } from "next"; import { headers } from "next/headers"; import Link from "next/link"; import { CertificateVerificationResult } from "@/components/public/CertificateVerificationResult"; import { BrandLogo } from "@/components/ui/BrandLogo"; import { requireLmsAdminClient } from "@/lib/lms/adminData"; import { verifyPublicAward } from "@/lib/lms/awardService"; import { permitCertificateVerification, privacySafeRequestKey } from "@/lib/lms/publicRateLimit";
export const dynamic = "force-dynamic"; export const metadata: Metadata = { title: "Certificate Verification · REALMS Institute" };
export default async function VerifyCertificateCodePage({ params }: { params: Promise<{ verificationCode: string }> }) { const { verificationCode } = await params; const requestHeaders = await headers(); const key = privacySafeRequestKey(`${requestHeaders.get("x-forwarded-for") ?? "unknown"}|${requestHeaders.get("user-agent") ?? "unknown"}`); const permitted = permitCertificateVerification(key); const result = permitted ? await verifyPublicAward(requireLmsAdminClient(), verificationCode) : null; return <main className="min-h-dvh bg-[#f5f2ea] px-5 py-12"><div className="mx-auto max-w-3xl"><BrandLogo className="size-16" sizes="64px" priority /><h1 className="mt-7 text-4xl font-semibold text-[#071327]">REALMS Award Verification</h1><div className="mt-7">{result ? <CertificateVerificationResult result={result} /> : <p className="rounded-2xl border border-amber-300 bg-amber-50 p-5">Please wait before submitting more verification requests.</p>}</div><Link href="/verify-certificate" className="mt-6 inline-flex font-semibold text-amber-800">Verify another award</Link></div></main>; }
